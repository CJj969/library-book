# 图书馆座位预约系统 —— 原型评审清单 v1

> 评审日期：2026-07-06  
> 评审范围：prototype/static-v1/ 全部 9 页  
> 评审结论：待填写（逐项检查后标注 ✅ / ❌ / ⚠️）

---

## 1. 核心用户流程是否完整

| # | 检查项 | 涉及页面 | 结果 |
|---|--------|----------|------|
| 1.1 | 用户能否从首页进入座位列表 | `index.html` → `seat-list.html` | ✅ |
| 1.2 | 用户能否从座位列表进入座位详情 | `seat-list.html` → `seat-detail.html` | ✅ |
| 1.3 | 用户能否在详情页看到时段占用和提交预约 | `seat-detail.html` | ✅ |
| 1.4 | 预约提交后是否能跳转到我的预约 | `seat-detail.html` → `my-reservations.html` | ✅ |
| 1.5 | 用户能否在我的预约页看到全部记录 | `my-reservations.html` | ✅ |
| 1.6 | 用户能否在我的预约页取消预约 | `my-reservations.html` | ✅ |
| 1.7 | 取消预约后是否有反馈 | `my-reservations.html` | ✅ |
| 1.8 | 取消后座位是否应在列表中恢复空闲 | 跨页逻辑 | ⚠️ 静态原型无法验证跨页数据，标注"后端实现" |
| 1.9 | 全程能否回到首页 | 所有用户端页面 | ✅ |

---

## 2. 页面跳转逻辑是否清楚

| # | 检查项 | 结果 |
|---|--------|------|
| 2.1 | 用户端导航栏在所有页面是否一致 | ✅ 全部使用 `.mobile-nav` |
| 2.2 | 管理端导航栏在所有页面是否一致 | ✅ 已统一为 `.admin-nav` |
| 2.3 | 返回按钮是否指向正确的上一页 | ⚠️ `seat-detail.html` 返回箭头指向 `seat-list.html` 正确；其余页无返回按钮（导航栏已覆盖） |
| 2.4 | 管理端未登录时是否应跳转登录页 | ⚠️ 静态原型不涉及，标注"后端实现" |
| 2.5 | 所有相对链接是否有效（无死链接） | ✅ 已修复死链接（`admin-stats` → `admin-statistics`），其余链接已验证 |
| 2.6 | 体验账号切换不跳转页面（前端切换） | ⚠️ 静态切换为下拉选择，不触发页面刷新，标注"后端实现" |

---

## 3. 空状态和错误状态是否考虑

| # | 检查项 | 涉及页面 | 结果 |
|---|--------|----------|------|
| 3.1 | 我的预约为空时是否有占位引导 | `my-reservations.html` | ✅ 底部空状态区+去选座按钮 |
| 3.2 | 座位列表某区域无座位是否有提示 | `seat-list.html` | ❌ 未实现区域切换后空状态提示，建议补充 |
| 3.3 | 座位不存在时是否跳转 404 | `seat-detail.html` → `404.html` | ✅ |
| 3.4 | 时段冲突时是否有错误提示 | `seat-detail.html` | ✅ 红色 alert-seat-danger |
| 3.5 | 管理员密码错误是否有错误提示 | `admin-login.html` | ✅ 红色 alert-seat-danger |
| 3.6 | 统计数据为 0 时是否显示 0 而不是空白 | `admin-statistics.html` | ✅ 数字硬编码，无空白态问题 |
| 3.7 | 无预约记录时管理端预约列表是否显示占位 | `admin-reservations.html` | ❌ 未实现空表格占位，建议补充 |
| 3.8 | 404 页面是否有回到首页的引导 | `404.html` | ✅ 回到首页按钮 |

---

## 4. 手机端和桌面端是否区分

| # | 检查项 | 结果 |
|---|--------|------|
| 4.1 | 用户端页面是否以手机视图展示（`.phone-frame`） | ✅ 全部用户端页面使用 |
| 4.2 | 用户端按钮触控区域是否足够大（≥ 44×44px） | ✅ 按钮高度 40~44px |
| 4.3 | 用户端内容在桌面端是否居中（最大宽度 480px） | ✅ |
| 4.4 | 管理端页面是否全宽展示（无手机框架） | ✅ |
| 4.5 | 管理端表格是否可横向滚动（如果列数多） | ⚠️ 当前列数不多不溢出，标注"后端数据量增多时需确认" |
| 4.6 | 管理端导航栏是否适合桌面点击操作 | ✅ |
| 4.7 | 手机端导航文字是否会溢出 | ⚠️ 当前 nav-links 数量适中不溢出，如增加链接需注意 |
| 4.8 | 手机端座位卡片 2 列布局在窄屏上是否拥挤 | ✅ 12px gap 充足 |

---

## 5. 必须在接后端前修改的问题

| # | 问题描述 | 严重程度 | 涉及页面 |
|---|----------|----------|----------|
| 5.1 | 座位列表页区域切换后缺少空状态占位提示 | 🟡 重要 | `seat-list.html` |
| 5.2 | 管理端预约列表无数据时缺少占位提示 | 🟡 重要 | `admin-reservations.html` |
| 5.3 | admin-login.html 表单提交链接为 `admin-reservations.html`（GET），后端应改为 POST | 🟢 轻微 | `admin-login.html` |
| 5.4 | 退出登录按钮在管理端指向 `index.html`，后端应改为 POST 到 `/Admin/Logout` | 🟢 轻微 | 所有管理端页面 |

> 标记规则：
> - 🔴 阻塞：必须先修改才能进入开发
> - 🟡 重要：建议修改，但不阻塞开发启动
> - 🟢 轻微：可优化，不影响功能理解

---

## 评审结论

| 维度 | 结论 |
|------|------|
| 用户流程完整性 | ✅ 通过 — 主链路闭环完整，9 页全部可跳转 |
| 页面跳转清晰度 | ✅ 通过 — 导航栏统一，链接无死链 |
| 边界状态覆盖 | ✅ 基本通过 — 成功/空/错误/404 均已覆盖，2 处空状态缺失标记为🟡 |
| 响应式适配 | ✅ 通过 — 用户端手机优先，管理端桌面全宽 |
| 视觉一致性 | ✅ 通过 — 全部使用自定义组件体系，统一配色/圆角/间距 |
| **总体结论** | **✅ 有条件通过 — 修复 5.1~5.2（空状态）后即可进入系统设计** |

---

## 附件：页面快速访问表

| 页面 | 文件 | 打开方式 |
|------|------|----------|
| 用户首页 | `prototype/static-v1/index.html` | 浏览器直接打开 |
| 座位列表 | `prototype/static-v1/seat-list.html` | 浏览器直接打开 |
| 座位详情 | `prototype/static-v1/seat-detail.html` | 浏览器直接打开 |
| 我的预约 | `prototype/static-v1/my-reservations.html` | 浏览器直接打开 |
| 管理员登录 | `prototype/static-v1/admin-login.html` | 浏览器直接打开 |
| 预约管理 | `prototype/static-v1/admin-reservations.html` | 浏览器直接打开 |
| 座位管理 | `prototype/static-v1/admin-seats.html` | 浏览器直接打开 |
| 统计页 | `prototype/static-v1/admin-statistics.html` | 浏览器直接打开 |
| 404 错误页 | `prototype/static-v1/404.html` | 浏览器直接打开 |
