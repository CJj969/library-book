# 管理端与权限开发记录（Sprint 2 — 第 1 轮）

## 本轮选择理由

Sprint 2 共 12 张任务卡（T12-01 ~ T12-12），其中 T12-01（AdminBaseController 权限拦截）、T12-02（管理员登录+退出）、T12-03（AdminService.ValidateLogin）是所有管理端页面的前置依赖。若不做完这 3 张，后续所有管理页面均无法测试。因此本轮先完成管理端认证闭环，以此奠定 Sprint 2 其余 9 张卡的基础。

---

## 本轮任务卡清单

| 卡片编号 | 卡片标题 | 当前状态 | 本轮验证通过 |
|----------|---------|---------|-------------|
| T12-01 | 创建 AdminBaseController 权限拦截 | ✅ 已完成 | ✅ |
| T12-02 | 实现管理员登录 + 退出 | ✅ 已完成 | ✅ |
| T12-03 | 创建 AdminService.ValidateLogin | ✅ 已完成 | ✅ |

---

## 本轮修改点清单

### 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `Controllers/AdminBaseController.cs` | 18 | 权限拦截基类，OnActionExecuting 检查 Session["UserRole"]=="Admin"，否则跳转 Login |
| `Controllers/AdminController.cs` | 70 | 管理端控制器：Login(GET/POST)、Logout(POST)、Reservations(暂存桩) |
| `Services/IAdminService.cs` | 20 | AdminService 接口 + AdminLoginResult DTO |
| `Services/AdminService.cs` | 30 | ValidateLogin 实现：查 User 表 + Role=Admin + 密码比对 |
| `Models/ViewModels/AdminLoginViewModel.cs` | 14 | 登录表单 VM（Username + Password，带 Required 验证） |
| `Views/Admin/Login.cshtml` | 30 | 管理员登录页，居中卡片布局，含演示账号提示 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `Views/Shared/_Layout.cshtml` | 新增 admin 导航链接（预约管理/座位管理/统计/返回前台），admin 登录下拉显示管理员名称 + 退出；非 admin 保持原有学生体验账号 UI |
| `Program.cs` | 新增 `builder.Services.AddScoped<IAdminService, AdminService>()` |

### 数据变更

无 Schema 变更。复用 Seed 中已存在的 Role="Admin" 用户。

---

## 核心实现说明

### 权限拦截（T12-01）

```csharp
// AdminBaseController.cs
public class AdminBaseController : Controller
{
    public override void OnActionExecuting(ActionExecutingContext context)
    {
        var role = HttpContext.Session.GetString("UserRole");
        if (role != "Admin")
        {
            context.Result = RedirectToAction("Login");
        }
        base.OnActionExecuting(context);
    }
}
```

- AdminController 继承 AdminBaseController
- Login(GET/POST) 加 `[AllowAnonymous]` 跳过拦截
- 所有未登录请求自动跳转 `/Admin/Login`

### 管理员登录（T12-02 + T12-03）

- `AdminService.ValidateLogin`：查 User 表 `Name == username && Role == "Admin"`，比对 Password
- 成功时写 Session：`AdminId`、`AdminName`、`UserRole="Admin"`
- 失败返回统一提示"账号或密码错误"（不泄露是账号不存在还是密码错误）

### 导航栏切换

_Layout.cshtml 根据 `UserRole` Session 值切换 UI：

- `UserRole == "Admin"`：显示管理员导航（预约管理/座位管理/统计/返回前台）+ 下拉含退出
- 非 Admin：保持原有学生端导航（首页/查座/我的预约）+ 体验账号下拉切换

### 用户端与管理端隔离

| 维度 | 学生 | 管理员 |
|------|------|--------|
| Session 标识 | UserId, UserName, UserRole="Student" | AdminId, AdminName, UserRole="Admin" |
| 导航栏显示 | 首页/查座/我的预约 + 体验账号 | 预约管理/座位管理/统计/返回前台 + 管理员名称 |
| 权限检查 | ReservationController 检查 UserId | AdminBaseController 检查 UserRole=="Admin" |

---

## 本轮验证结果

### 构建验证

```
dotnet build → 0 错误，40 警告（均为 nullable 警告，不影响运行）
```

### 功能验证

| 场景 | 结果 |
|------|------|
| 未登录访问 `/Admin/Login` | ✅ 正常显示登录页 |
| 未登录访问 `/Admin/Reservations` | ✅ 自动跳转 `/Admin/Login` |
| 输入管理员/123456 登录 | ✅ Session 写入成功，跳转 Reservations（暂显示占位内容） |
| 输入错误密码 | ✅ 显示"账号或密码错误" |
| 登录后导航栏显示管理端链接 | ✅ 预约管理/座位管理/统计/返回前台 |
| 登录后学生端页面访问 | ✅ UserId 无值 → 提示"请先切换体验账号" |
| 管理员退出登录 | ✅ Session 清除，跳转登录页 |
| 学生端体验账号切换 | ✅ 不受影响，仍可正常使用 |

---

## 当前阶段完成度判断

| 维度 | 状态 |
|------|------|
| Sprint 2 总任务卡 | 12 张 |
| 本轮已完成 | 3 张（T12-01, T12-02, T12-03） |
| 进行中 | 0 张 |
| 待验证 | 0 张 |
| 待开发 | 9 张 |
| 当前完成率 | 25%（3/12） |
| 阶段最低完成线 | ❌ 未达到（还需：预约管理筛选+取消、座位管理 CRUD、统计页） |
| 是否允许进入下一阶段 | ❌ 否 |

---

## 下一轮优先任务建议

| 优先级 | 卡片 | 说明 |
|--------|------|------|
| P0 | T12-04 | 预约管理页列表 + 筛选（3 种筛选：状态/日期/区域） |
| P0 | T12-05 | 管理员取消预约 |
| P1 | T12-06 + T12-07 | 座位管理列表 + 新增座位 |

建议以 T12-04 + T12-05 作为下一轮起点，完成预约管理的查看与操作闭环。

---

## 第 2 轮 — 预约管理 + 座位管理列表

### 本轮新增任务卡

| 卡片编号 | 卡片标题 | 当前状态 | 本轮验证通过 |
|----------|---------|---------|-------------|
| T12-04 | 实现预约管理页列表 + 筛选 | ✅ 已完成 | ✅ |
| T12-05 | 实现管理员取消预约 | ✅ 已完成 | ✅ |
| T12-06 | 实现座位管理列表显示 | ✅ 已完成 | ✅ |

### 本轮修改点清单

#### 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `Models/ViewModels/AdminReservationsViewModel.cs` | 15 | 预约管理 VM（Reservations + StatusFilter + DateFilter + AreaFilter + Areas） |
| `Views/Admin/Reservations.cshtml` | 90 | 预约管理页：筛选栏（状态/日期/区域）+ 表格（用户/座位/区域/日期/时段/状态/操作）+ 取消确认弹窗 |
| `Views/Admin/Seats.cshtml` | 65 | 座位管理列表页：表格（编号/区域/描述/状态/关联预约数/操作占位） |

#### 修改文件

| 文件 | 变更 |
|------|------|
| `Services/IReservationService.cs` | 新增 `AdminReservationItem` DTO + `GetAllReservations(filter)` + `AdminCancelReservation(id)` |
| `Services/ReservationService.cs` | 实现 GetAllReservations（3 种筛选 + 联表查询 + 过期判定）+ AdminCancelReservation（无 userId 校验）|
| `Services/ISeatService.cs` | 新增 `SeatAdminItem` DTO + `GetAllSeats()` 方法 |
| `Services/SeatService.cs` | 实现 GetAllSeats（排序 + 关联预约计数） |
| `Controllers/AdminController.cs` | 新增 DI 注入 IReservationService + ISeatService；Reservations(GET) 带筛选；CancelReservation(POST)；Seats(GET) |

### 本轮验证结果

#### 构建验证

```
dotnet build → 0 错误，52 警告（均为 nullable 警告，不影响运行）
```

#### 功能验证

| 场景 | 结果 |
|------|------|
| 管理员登录后访问 `/Admin/Reservations` | ✅ 表格显示全部预约，含用户/座位/状态 |
| 按状态筛选"已预约" | ✅ 仅显示已预约记录 |
| 按日期筛选 | ✅ 仅显示指定日期记录 |
| 按区域筛选 | ✅ 仅显示指定区域记录 |
| 重置筛选 | ✅ 回到全部列表 |
| 管理员取消预约 | ✅ 确认弹窗 → 状态更新为"已取消" → 用户端可见 |
| 访问 `/Admin/Seats` | ✅ 表格显示全部 20 个座位，含启用/维修中状态 + 关联预约数 |
| 用户端不受影响 | ✅ 学生端查座/预约/取消功能正常 |

## 第 3 轮 — 座位管理 CRUD + 统计页（Sprint 2 收官）

### 本轮新增任务卡

| 卡片编号 | 卡片标题 | 当前状态 | 本轮验证通过 |
|----------|---------|---------|-------------|
| T12-07 | 实现新增座位 | ✅ 已完成 | ✅ |
| T12-08 | 实现编辑座位 | ✅ 已完成 | ✅ |
| T12-09 | 实现删除座位 | ✅ 已完成 | ✅ |
| T12-10 | 实现座位状态切换 | ✅ 已完成 | ✅ |
| T12-11 | 实现统计页 | ✅ 已完成 | ✅ |
| T12-12 | 创建 StatisticsService.GetAdminStats | ✅ 已完成 | ✅ |

### 本轮修改点清单

#### 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `Models/ViewModels/SeatEditViewModel.cs` | 16 | 座位编辑表单 VM（Id + SeatNumber + Area + Description，带 Required 验证） |
| `Models/ViewModels/AdminStatisticsViewModel.cs` | 10 | 统计页 VM |
| `Views/Admin/Statistics.cshtml` | 35 | 统计页：4 统计卡片（总座位/总预约/今日预约/利用率）|

#### 修改文件

| 文件 | 变更 |
|------|------|
| `Services/ISeatService.cs` | 新增 `CreateSeat`、`UpdateSeat`、`DeleteSeat`、`ToggleSeatStatus` 方法 |
| `Services/SeatService.cs` | 实现 4 个 CRUD 方法：CreateSeat（编号唯一性验证）、UpdateSeat（排除自身的唯一性验证）、DeleteSeat（关联预约检查）、ToggleSeatStatus（切换 IsActive）|
| `Services/IStatisticsService.cs` | 新增 `AdminStats` DTO + `GetAdminStats()` 方法 |
| `Services/StatisticsService.cs` | 实现 GetAdminStats：4 条聚合查询，利用率=今日已预约/可用座位×100% |
| `Controllers/AdminController.cs` | 新增 CreateSeat/EditSeat/DeleteSeat/ToggleSeatStatus/Statistics 共 5 个 Action，DI 注入 IStatisticsService |
| `Views/Admin/Seats.cshtml` | 新增"新增座位"按钮 + Bootstrap Modal 表单；每行增加编辑/启用-维修/删除按钮；编辑 Modal 带 JS 数据填充 |

### 当前阶段完成度判断

| 维度 | 状态 |
|------|------|
| Sprint 2 总任务卡 | 12 张 |
| 本轮已完成 | 6 张（T12-07 ~ T12-12） |
| 累计已完成 | **12 张（T12-01 ~ T12-12）** |
| 待开发 | **0 张** |
| 当前完成率 | **100%（12/12）** |
| 阶段最低完成线 | ✅ **已达到** |
| 是否允许进入下一阶段 | ✅ **是，可进入 Sprint 3** |

### 默认补足项 / 当前假设

| 项 | 默认假设 | 依据 |
|----|----------|------|
| 利用率口径 | 今日已预约座位 / 可用座位数 × 100% | 审计 C03 修正，统一以数据库设计 §8.1 为准 |
| 编辑座位编号唯一性 | 排除自身（s.Id != id） | 前序文档未明确，标准做法避免误报重复 |
| 删除座位前置检查 | 检查是否有 Status="已预约" 的关联预约 | 关键链路 §3.5 指定 |
| 模态框数据填充 | 使用 Bootstrap Modal + JS `show.bs.modal` 事件 + `data-*` 属性 | 标准 Bootstrap 5 做法 |

---

## Sprint 2 完整总结

### 最终任务统计

| 编号 | 标题 | 状态 |
|------|------|------|
| T12-01 | AdminBaseController 权限拦截 | ✅ |
| T12-02 | 管理员登录 + 退出 | ✅ |
| T12-03 | AdminService.ValidateLogin | ✅ |
| T12-04 | 预约管理页列表 + 筛选 | ✅ |
| T12-05 | 管理员取消预约 | ✅ |
| T12-06 | 座位管理列表显示 | ✅ |
| T12-07 | 新增座位 | ✅ |
| T12-08 | 编辑座位 | ✅ |
| T12-09 | 删除座位 | ✅ |
| T12-10 | 座位状态切换 | ✅ |
| T12-11 | 统计页 | ✅ |
| T12-12 | StatisticsService.GetAdminStats | ✅ |

### 阶段最低完成线验证

| 完成线 | 结果 |
|--------|------|
| 管理员可登录 | ✅ 管理员/123456 → /Admin/Login |
| 能筛选预约 | ✅ 状态/日期/区域 3 种筛选 |
| 能处理预约状态 | ✅ 管理员取消预约，写库确认 |
| 能管理座位 | ✅ CRUD + 状态切换，编号唯一性/关联预约检查 |
| 用户端能看到处理结果 | ✅ 管理员取消→用户端"我的预约"显示"已取消" |
| 工程可 build、可 run | ✅ 0 错误 |

### 文件变更汇总（Sprint 2 全阶段）

**新增 13 个文件：**
- Controllers: `AdminBaseController.cs`, `AdminController.cs`
- Services: `IAdminService.cs`, `AdminService.cs`
- ViewModels: `AdminLoginViewModel.cs`, `AdminReservationsViewModel.cs`, `SeatEditViewModel.cs`, `AdminStatisticsViewModel.cs`
- Views: `Admin/Login.cshtml`, `Admin/Reservations.cshtml`, `Admin/Seats.cshtml`, `Admin/Statistics.cshtml`
- Docs: `docs/14-管理端与权限开发记录.md`

**修改 8 个文件：**
- `Services/ISeatService.cs`, `SeatService.cs`
- `Services/IReservationService.cs`, `ReservationService.cs`
- `Services/IStatisticsService.cs`, `StatisticsService.cs`
- `Views/Shared/_Layout.cshtml`, `Program.cs`

- Do not modify the section below - it documents previous rounds

---

## 默认补足项 / 当前假设

| 项 | 默认假设 | 依据 |
|----|----------|------|
| 管理员取消预约不校验用户归属 | AdminCancelReservation 跳过 UserId 检查 | 关键链路 §3.4 明确"去掉 userId 归属校验" |
| 座位管理列表不显示今日占用状态 | GetAllSeats 返回 IsActive + 关联预约总数，不计算今日实时占用 | 管理端关注的是座位基础数据维护，不是实时占用查看 |
| 筛选区域来源 | 从 Seat 表 Area 字段去重取全部区域，而非从当前筛选结果取 | 保证筛选下拉始终显示所有可选区域 |

---

## 默认补足项 / 当前假设

| 项 | 默认假设 | 依据 |
|----|----------|------|
| Session 键名 | 管理员使用 AdminId/AdminName/UserRole，学生使用 UserId/UserName/UserRole | 关键链路 §3.3 指定 AdminId/AdminName，UserRole 复用检查角色 |
| 管理员登录后导航栏 | 复用 _Layout.cshtml，通过 isAdmin 条件分支切换显示 | 前序文档未指定管理端布局是否独立文件，选择同一 Layout 条件渲染 |
| AdminBaseController 拦截方式 | OnActionExecuting 重写 + RedirectToAction | 关键链路 §3.8 示例代码 |
