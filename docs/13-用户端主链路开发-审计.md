# 用户端主链路开发审计报告（Sprint 1 → Stage 13）

> 审计基准：docs/05、docs/08、docs/09、docs/12、项目任务板
> 审计对象：README.md、docs/13-用户端主链路开发记录.md、docs/项目任务板与迭代记录.md、src/
> 审计范围：仅阶段13新增结果，不扩大至阶段12或更早

---

## A. 总体判断

**Sprint 1（用户端主链路）实现质量合格，可以进入 Sprint 2（管理端、权限与状态回流）。**

本轮实现的核心逻辑（查座→详情→预约→我的预约→取消）完整对齐 docs/08 和 docs/09 的设计规格：校验顺序、字段名、状态值、错误消息均一致，且真实经过数据库写入与读取验证。无 P0/P1 阻塞项。

---

## B. 发现的问题清单

### B1. 代码质量问题

| 编号 | 文件 | 问题 | 严重程度 | 关联文档 |
|------|------|------|---------|---------|
| S13-D1 | `Views/Seat/Detail.cshtml:48` | POST 表单均无 `@Html.AntiForgeryToken()`，对应 Action 也无 `[ValidateAntiForgeryToken]` | **P2（可优化）** | 未在任何前序文档中明确要求防伪造令牌，但属于安全最佳实践 |
| S13-D2 | `Views/Reservation/MyReservations.cshtml:20-26` | 状态标签使用 Bootstrap 默认类（bg-primary/bg-secondary/bg-info），与 docs/05 §6.6 规范的色值（Blue-600/Red-600/Emerald-600）不一致 | **P3（视觉对齐）** | docs/05 §6.6 |
| S13-D3 | `Controllers/ReservationController.cs:86` | `Switch` 动作直接注入 `AppDbContext` 而非通过 Service，与其他动作的 DI 方式不一致 | **P3（代码一致性）** | 无前序文档指定 |
| S13-D4 | `Models/ViewModels/CreateReservationViewModel.cs` | 仅 `[Required]` 属性验证，缺少对 TimeSlot 合法值（上午/下午/晚上）的模型层校验 | **P3（边界优化）** | docs/09 §2.5（Service 层已兜底校验） |
| S13-D5 | 项目文件 | 36 个 nullable 警告未处理，主要源于 DTO 属性缺少 `?` 标记（如 `SeatItem.SeatNumber` 等） | **P3（代码质量）** | — |

### B2. 文档一致性

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|---------|------|
| S13-D6 | 任务板 §0.3 中 C03/C04/C06 标注为 "✅ 已处理" 但未经 "👁 已复核" 确认 | **P3（任务板完整性）** | 不影响 Sprint 2 启动，但建议 Sprint 2 开始前完成复核标记 |

### B3. 未覆盖设计文档的项

| 编号 | 设计规格 | 实际状态 | 严重程度 |
|------|---------|---------|---------|
| S13-D7 | docs/05 PC-01：桌面端最大宽度 480px 居中 | 当前 `_Layout.cshtml` 未限制内容区最大宽度，`container` 类使用 Bootstrap 默认 1320px 断点 | **P3（视觉对齐，Spring 3）** |
| S13-D8 | docs/05 PC-02：空闲座位卡片色值规范（Emerald-600 `#059669`） | 使用 Bootstrap `bg-success`（`#198754`），不精确匹配 | **P3（视觉对齐，Spring 3）** |
| S13-D9 | docs/05 §6.6：状态标签背景色规范 | 均为色值级规范，当前使用 Bootstrap 语义类替代，色值不精确匹配 | **P3（视觉对齐，Spring 3）** |

---

## C. 必改项

**无。** 未发现 P0/P1 级别必须在本轮修复的问题。

> 说明：S13-D1~D9 均为 P2/P3 等级，不影响功能正确性、不影响用户闭环完整性、不阻塞 Sprint 2 开发。视觉对齐类问题（D2/D7/D8/D9）统一归入 Sprint 3 处理。

---

## D. 建议优化项

| # | 建议 | 关联编号 | 建议处理时机 |
|---|------|---------|------------|
| R1 | POST 表单统一添加 AntiForgeryToken + ValidateAntiForgeryToken | D1 | Sprint 2（作为安全基线随管理端一起） |
| R2 | 状态标签色值改为 Bootstrap 自定义 CSS（site.css 覆盖）以对齐 docs/05 | D2 | Sprint 3 |
| R3 | 将 `Switch/Logout` 抽出为 `SessionController` 或统一归入 `AccountService` | D3 | Sprint 2（与管理端登录合并重构） |
| R4 | docs/13 补写"默认补足项"：POST 无防伪造令牌的安全边界说明 | D1 关联 | Sprint 1 关闭前补录 |
| R5 | 任务板 §0.3 C03/C04/C06 补充复核标记 | D6 | Sprint 2 启动前 |

---

## E. 是否可以进入下一步"管理端、权限与状态回流"

**✅ 是。** 满足进入 Sprint 2 的条件：

1. **Sprint 1 完成度**：12/12 张任务卡全部完成，完成率 100%
2. **阶段最低完成线**：✅ 用户端 4 页全部可访问，预约/取消数据真实写入 DB，全链路闭环已验证
3. **P1 修订项**：C03/C04/C06 均标注为"已处理"（Sprint 2 进入前置条件）
4. **README 同步**：已从阶段 10/12 口径更新为 Sprint 1 完成状态
5. **未阻塞项**：所有发现的问题均为 P2/P3，不阻塞下一阶段

**建议 Sprint 2 的首批任务**（按依赖顺序）：
1. `T12-01` — AdminBaseController 权限拦截基类（所有管理页前置条件）
2. `T12-02` — 管理员登录 + 退出（权限基础）
3. `T12-03` — 合并重构 Session 管理（Switch/Logout 与 AdminLogin 统一）
4. 然后依次推进 T12-04~T12-12
