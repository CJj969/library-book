# 验收测试报告

## 测试范围

依据 PRD §10 验收标准（T01–T17）逐条测试，补充边界情况测试。

---

## 用户端验收

| 编号 | 操作 | 预期结果 | 结果 | 备注 |
|------|------|----------|------|------|
| T01 | 启动项目，浏览器访问首页 | 首页正常渲染，显示欢迎语和座位概况 | ✅ 通过 | HTTP 200，显示 stats |
| T02 | 点击"进入座位列表" | 显示 ≥20 个座位卡片，颜色区分状态 | ✅ 通过 | 种子 20 个，空闲绿/已约灰/维修黄 |
| T03 | 点击空闲座位卡片 | 进入座位详情页，显示座位信息和时段占用 | ✅ 通过 | HTTP 200，时段卡正确显示占用 |
| T04 | 选择时段 → 点击提交预约 | 提示预约成功，跳转到"我的预约" | ✅ 通过 | TempData 提示 + 重定向 |
| T05 | 在"我的预约"页 | 刚预约的记录可见，状态为"已预约" | ✅ 通过 | 列表正确渲染 |
| T06 | 点击该预约的"取消"按钮 → 确认 | 预约状态变为"已取消" | ✅ 通过 | POST 取消 + 刷新列表 |
| T07 | 回到座位列表 | 该座位恢复"空闲"状态 | ✅ 通过 | 该日期无活跃预约即空闲 |
| T08 | 导航栏切换体验账号（A → B） | B 的"我的预约"为空（隔离正常） | ✅ 通过 | Session 切换，数据隔离 |
| T09 | 尝试预约已被占的时段 | 提示"该时段已被预约" | ✅ 通过 | 冲突检测返回错误提示 |

## 管理端验收

| 编号 | 操作 | 预期结果 | 结果 | 备注 |
|------|------|----------|------|------|
| T10 | 未登录时访问 `/Admin/Reservations` | 自动跳转到登录页 | ✅ 通过 | HTTP 302 → `/Admin/Login` |
| T11 | 输入 admin / 123456 | 登录成功，进入预约管理页 | ✅ 通过 | HTTP 200，Session 写入 |
| T12 | 预约管理页 | 显示所有用户的预约记录 | ✅ 通过 | 含区域/日期/状态三筛选 |
| T13 | 手动取消一条预约 | 用户端该预约状态同步变为"已取消" | ✅ 通过 | AdminCancelReservation 跳过 userId |
| T14 | 进入座位管理 → 新增一个座位 | 用户端座位列表立即可见 | ✅ 通过 | CreateSeat 后刷新列表 |
| T15 | 禁用某个座位 | 用户端该座位显示"维修中"，不可预约 | ✅ 通过 | ToggleSeatStatus → IsActive=false |
| T16 | 进入统计页 | 显示预约总数和座位利用率数字 | ✅ 通过 | 利用率 = 今日已约 / 可用座位 × 100% |
| T17 | 访问不存在的 URL | 显示 404 错误页，有返回首页链接 | ✅ 通过 | HTTP 404，"页面未找到" + 回到首页链接 |

## 边界情况测试

| 编号 | 场景 | 操作 | 预期结果 | 结果 | 备注 |
|------|------|------|----------|------|------|
| B01 | 预约冲突 | 同一座位同一时段重复预约 | 提示"该时段已被预约" | ✅ 通过 | Unique Index 约束 |
| B02 | 过去时段预约 | 选择当前时间之前的时段 | 不可选（不提供过去时段） | ✅ 通过 | 时段按当日 hardcode 渲染 |
| B03 | 取消已取消的预约 | 对已取消的预约再次点击取消 | 合理提示或静默处理 | ✅ 通过 | 取消后状态=已取消，再次取消返回错误 |
| B04 | 删除有活跃预约的座位 | 管理员删除有预约的座位 | 提示有活跃预约，拒绝删除 | ✅ 通过 | DeleteSeat 检查活跃预约 |
| B05 | 未登录用户直连 URL | 访问 `/Seat/Detail/1`、`/Reservation/Create` | 详情页可看；预约需登录 → 提示 | ✅ 通过 | Session 无 userId 时提示 |
| B06 | 同一座位同天多时段 | 上午预约→下午预约同一座位 | 均成功（不同时段不冲突） | ✅ 通过 | 唯一索引含 (SeatId,Date,Slot) |
| B07 | 管理员退出 | 点击退出按钮 | 清除 Session，跳转登录页 | ✅ 通过 | Logout 清除三个 key |
| B08 | 管理员直接访问用户端 | 已登录 admin 访问 `/Seat` | 正常渲染，导航显示当前角色 | ✅ 通过 | 共用 layout，角色判断 |

## 测试结论

**全部 25 项测试通过（17 PRD + 8 边界）。** Sprint 3 全链路功能正常。

## 测试环境

- 运行时：.NET 6.0.428 / SQLite
- 浏览器：Chrome 最新版
- 测试方式：curl + 浏览器手动验证
- 数据：种子数据（5 用户 + 20 座位 + 3 时段/日）
